<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'License Manager' %></title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="page-wrapper">
        <% if (typeof user !== 'undefined' && user) { %>
        <aside class="sidebar">
            <div class="sidebar-header">
                <h2>License Manager</h2>
            </div>
            <nav class="main-nav">
                <ul>
                    <li><a href="/dashboard">📊 Dashboard</a></li>
                    <li><a href="/licenses">📄 View Licenses</a></li>
                    <li><a href="/add-license">➕ Add License</a></li>
                    <li><a href="/logout" class="logout-link">🚪 Logout (<%= user %>)</a></li>
                </ul>
            </nav>
        </aside>
        <% } %>

        <div class="main-content <%= typeof user === 'undefined' || !user ? 'no-sidebar' : '' %>">
            <% if (typeof user !== 'undefined' && user) { %>
            <header class="content-header">
                <h1><%= title || 'License Manager' %></h1>
            </header>
            <% } %>
            
            <main class="content-body">
                <%- body %>
            </main>
        </div>
    </div>
    <script src="/script.js"></script>
</body>
</html>